# Changelog

本地应用安装器的版本更新记录。版本号格式：`v<主>.<次>`，每次公开发布递增。

## v1.1.3 — 2026-09-17

### 新增

- **页面底部署名行**：`© 2026 应用安装 v<版本> · Crafted by 西了个瓜`，
  与风扇调速等其它自装应用统一风格。
- **`tools/verify.py`**：安装 / 重启后一键验证 —— 面板是否在线、闸门处于哪个状态、
  未登录时 `/api/*` 与 `/files/` 是否都被拦住、环境自检逐项结果、已上传应用与
  本地包库清单。退出码非 0 即有问题，可直接串进部署脚本。
- `.gitignore` / `.gitattributes`：仓库标准化。**`.env` 已加入忽略** ——
  它里面有 `TPK_PASSWORD`，误提交等于把页面口令一起公开。

关于版本号：展示串由源码里的 `VERSION` 常量提供（含 `v` 前缀，与 fanctl 的
`VERSION = "v1.73"` 同做法），页脚用 `__VERSION__` 占位、启动时注入。
打包脚本 `tie-niu-led/appstore/_build_appinstall.py` 会断言它与 `config.json` 的
`version` 一致（比较时忽略 `v` 前缀），避免出现「包是新版本、页脚还写着旧版本」
这种只靠肉眼很难发现的漂移。

### 变更

- **三个数据路径支持环境变量覆盖**：`docker-compose.yml` 里 `TPK_STORE`、
  `TPK_WEBROOT` 与数据库目录此前是写死的，`.env` 里填了不生效 —— 与"路径全部
  可用环境变量覆盖"的说法不符。现在改成 `${变量:-默认值}`，并且 `volumes` 跟着
  同一个变量走，不用再手改两处。
- **`config.json` 与打包脚本统一**：仓库这份此前 `installParameters` 是空的，
  而 tpk 里声明了 `TPK_PASSWORD`；两边还各留了一版描述，属于互相漂移。现已合并，
  并在打包脚本里加断言 —— 两处不一致直接在打包那一刻报错。
- README 补齐「适用机型与系统」章节，与灯控中心 / 风扇调速两个仓库的格式对齐。

### 修复

- 页脚版本号补上 `v` 前缀（首版误写成 `1.1.3`）。
- **`tools/check_page.py` 漏检一半内联事件**：它只在 `<script>` 内容里搜 `onclick=`，
  而静态写法（`onclick="logout()"`）写在 HTML 标签上、不在 script 里 ——
  页面里 6 个 handler 实际只查了 3 个，漏掉的那几个即便函数名写错也发现不了。
  已改为在整个页面上搜。同时新增：`--live` 模式下页面里若还残留 `__VERSION__`
  占位符直接报错（说明线上跑的是不注入的旧文件）。

## v1.1.2 — 2026-09-17

### 新增

- **手机端适配**：此前媒体查询只有一行（只管了标题栏的浮动按钮），表格、输入框、按钮都是桌面尺寸，
  手机上横向溢出、按钮难点。现在窄屏（≤640px）下：表格改为**一行一项的卡片式**
  （表头隐藏，每个字段前带名称标签）、输入框铺满且字号 16px（小于 16px 会被 iOS 自动放大整个页面）、
  主按钮与「退出登录」铺满宽度、点击目标加大。

### 修复

- **手机上点上传区没反应**：原写法是 `display:none` 的 `<input type=file>` + JS 调 `.click()`。
  移动端浏览器与应用内 WebView 会拦掉对隐藏 file input 的程序化点击。现在 input 用
  `opacity:0` + 绝对定位**铺满整块虚线区域**，用户点的就是 input 本体 —— 纯原生行为，
  不依赖任何 JS 调用。
- **手机上选择文件时浏览器退回**：根因是 `accept=".tpk"`。`.tpk` 是非标准扩展名，
  安卓侧要解析 MIME，部分 WebView 在拉起选择器时会直接崩掉（表现就是浏览器闪退）。
  现在触屏设备上一律移除 `accept`（类型校验交给服务端），桌面端保留以便筛文件。
- `input` 移到虚线区域内部后，原先三处 `drop.innerHTML=…` 会把 input 一起删掉，
  已改为只更新内部的文字节点。

### 说明

- 少数环境仍可能传不了文件，通常是宿主 App 的限制而非本程序：**NAS 官方 App 内置打开的页面
  （WebView）是否支持选文件，取决于 App 自身有没有实现文件选择回调**。页面检测到触屏会
  给出提示；改用手机自带浏览器（Chrome / Safari / Edge）或电脑打开同一地址即可。

## v1.1.1 — 2026-09-17

### 新增

- **首次打开自行设置口令**：装完不必再去找口令。页面第一次打开时直接显示「设置访问口令」，
  自己定一个（≥6 位）就能用 —— **不需要 root 权限**。此前口令是随机生成后写进
  `/userdata/tpk_local/访问口令.txt`，而那是 `root:root 600`，非 root 用户读不到、
  页面又进不去，等于把自己锁在门外。
- **登录后可改口令**：新增「访问口令」卡片。改口令会重新生成会话密钥，
  其它设备上的登录立刻失效，需要重新输一次。

### 修复

- **页面停在全屏「加载中」**（v1.1.0 引入）：本地包库表格里内联 `onclick` 的转义单引号
  少写了一个反斜杠 —— `PAGE` 是非 raw 三引号字符串，源码里的 `\'` 到运行时只剩 `'`，
  整段 `<script>` 语法错误，浏览器里一行 JS 都没执行（登录卡片因此也不出现）。
  现在所有按钮改用属性绑定事件；并新增页面体检工具 `tools/check_page.py`，
  支持 `--live` 直接检查线上实际下发的页面，防止这类错误再次外发。
- 页面里加了独立的自诊断脚本块：脚本万一又整体出错，4 秒后会直接提示「页面脚本没能启动」，
  而不是让用户对着「加载中」猜。

### 说明

- 卸载重装**不会**重置口令：`$TPK_STORE/.auth.json` 跟着数据目录走，升级不打扰。
  忘了口令只能请有 root 权限的人删掉该文件并重启容器。
- 只有显式设置 `TPK_PASSWORD` 时才写 `访问口令.txt`（保留给老部署的兼容路径）。

## v1.1.0 — 2026-09-17

### 新增

- **访问口令**：页面与写接口现在需要口令才能用。这个页面安装的应用会以 root 在宿主机上运行，
  之前局域网内任何设备打开 8978 就能拖包安装，等于把机器交出去。口令默认随机生成、
  写在 `/userdata/tpk_local/访问口令.txt`（权限 600），也可用 `TPK_PASSWORD` 指定；
  登录后下发 HMAC 签名的 HttpOnly Cookie，有效期 7 天，重启不掉线；连续输错会递增等待时间。
- **本地包库管理**：列出 NAS 上留存的全部 `.tpk`（含已卸载、未注册的），显示应用名、版本、
  大小、上传时间、MD5 与应用中心里的实时状态；支持下载、重装、删除。
  删除是把包移入 `trash/` 子目录而不是真删；应用仍在运行或安装中时不允许删包。
- **环境自检页**：一键检查包目录可写、应用中心数据库字段是否齐全、9004 接口是否可达、
  图标对外目录、本机回环访问，并列出当前全部路径配置 —— 换 NAS 时用来看哪里不兼容。

### 修复

- **图标公网发布失效**：`TPK_PUBLIC_BASE` 默认值被误改成空串，导致新装应用的图标写成局域网
  地址，手机 App 与远程网页显示白图（PC 内网看不出来）。已恢复为铁牛对外代理地址，
  并支持显式填 `off` 关闭。
- **容器时区**：compose 缺少 `TZ=Asia/Shanghai`，包列表时间比本地时间少 8 小时。
- 安装流程从请求处理里抽出为公共函数，"上传安装"与"用本地包重装"走同一条路径。
- **页面脚本整个失效（本地包库那版）**：新增的包库表格里，内联 `onclick` 里的转义单引号
  少写了一个反斜杠。`PAGE` 是**非 raw** 的三引号字符串，源码写 `\'` 运行时只剩 `'`，
  输出的 JS 变成 `onclick="reinstall(''+p.code+'',this)"` —— 相邻字符串字面量 →
  **整个 `<script>` 语法错误 → 浏览器里一行 JS 都不执行**：登录卡片不出现、
  两个列表永远停在「加载中」，而服务端接口用 curl 测全是好的。
  改为与同文件 `unins(...)` 一致的 `\\'` 写法。

### 变更

- 路径、端口、数据库、应用中心接口全部支持环境变量覆盖，Docker 部署包可直接用于同类 NAS。
- 新增 `tools/check_sync.sh`：比对本地源码 / 容器运行版 / 主机兜底版三份代码是否同源。
- 新增 `tools/check_page.py`：页面脚本体检。它把 `PAGE` 字面量交给 `ast.literal_eval` 求值
  —— 得到与运行时**逐字节相同**的 HTML —— 再跑 `node --check` 查语法，
  并核对 `getElementById` 的 id 是否齐全、内联 `onclick` 调用的函数是否都定义过、
  有没有"被吃掉的转义"痕迹。支持 `--live <url>` 直接检查**线上实际下发**的页面：
  源码通过不代表线上是对的（部署了旧文件或忘了重启容器，症状和写错代码一模一样）。

  > 上一个版本就是被这类问题咬的：只看接口（curl/python 全 200），
  > 看不出浏览器里一行 JS 都没跑起来。改完页面**必须**跑一遍
  > `tools/check_page.py --live`，再用真浏览器看一眼。

## v1.0.2 — 2026-09-16

### 变更

- 作者名统一署「西了个瓜」。
- 应用中心「镜像大小」字段改为真实值（121437 KiB，与其它共用基础镜像的应用一致）。
- 新增 Docker 部署包：路径全部可用环境变量覆盖，图标发布支持自定义公网前缀。

## v1.0.1 — 2026-09-15

### 变更

- 图标发布改动：图标同步到对外网页目录，手机 App 与远程网页不再白图。
- 启动自愈：容器启动时重新发布已上传应用的图标，并修正数据库里的局域网图标地址。

## v1.0.0 — 2026-09-14

### 新增

- 首个版本：网页拖拽上传 `.tpk`，自动注册进应用中心并触发安装 / 升级，支持一键卸载。
