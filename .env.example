# 环境变量样例：复制为 .env 后按需修改；不复制也能跑（全部使用下面的默认值）。
# 默认值即铁牛OS / ZeroNAS / 铁牛NAS 的实际路径。

# 面板监听端口（容器 host 网络下直接占用宿主机端口）
TPK_PORT=8978

# 应用中心 API 地址（安装/卸载/状态都走它）
TPK_APPSTORE_API=http://127.0.0.1:9004/appstoreApi

# 安装位置下拉框的默认值：必须填存储池路径
TPK_DEFAULT_LOC=/volume1

# 应用中心"镜像大小"显示值，单位 KiB（界面按 /1024 显示成 MB）
# 基础镜像 python:3.12-slim-bookworm 实测 124,351,708 B ≈ 121,437 KiB
TPK_IMAGE_SIZE_KB=121437

# 图标默认以 base64 内嵌进数据库（与访问入口、协议、客户端都无关，
# 局域网 / 中转 / 手机 App 都能显示），一般**不需要**填这一项。
# 只有某个客户端不认 data URL 时，才在这里填**这台机器自己**的公网前缀
# （形如 https://nas.example.com/pc —— 注意要自带 /pc，别填成别人的域名）。
# 显式填 off 表示不把图标发布到对外网页目录。
TPK_PUBLIC_BASE=

# 访问口令。留空 = 首次打开页面时由访问者自行设置（不需要 root 权限），
# 之后记录在 /userdata/tpk_local/.auth.json（只存哈希，不含明文）。
# 忘了口令又没有 root：请有 root 的人删掉该文件并重启容器 appinstall-app。
# 在这里填一个则优先级最高，每次启动都会以它为准。
# 想让安装包里的配置直接生效，就在这里写一个固定口令。
# 为什么需要它：.tpk 里的安装脚本是以 root 在宿主机跑的，"能打开这个页面"等价于
# "能以 root 执行任意命令"，所以 8978 不能裸奔。
TPK_PASSWORD=

# 下面三项是数据路径。它们在 docker-compose.yml 里**同时**用于「程序读的环境变量」
# 与「目录挂载」，两处都取这里的值 —— 所以改这里就够了，不用再去动 compose。
# 容器内外路径保持相同（NAS 上习惯这么放），换地方时按整目录替换。

# 上传包与图标缓存的存放目录
TPK_STORE=/userdata/tpk_local

# 应用中心数据库所在目录；程序读的库文件路径由它拼成 <目录>/appstore.db。
# Docker 部署不用单独设 TPK_DB，宿主机直接跑本程序时才需要指定文件路径。
TPK_DB_DIR=/userdata/db

# 图标发布到的对外网页目录（nginx 的 /pc/ 指向它）
TPK_WEBROOT=/usr/local/pc
